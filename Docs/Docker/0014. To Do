### **`-it`**

* Combines `-i` (**interactive**) and `-t` (**TTY**), allowing terminal interaction.

```bash
docker run -it ubuntu bash
```

---

### **`ps`**

What is Docker compose, docker desktop
docker compose up -d.
docker compose down
 
Steps to create a docker container 

Application of docker 

What is docker exec
What is docker network 
What is usually shared in docker that can be built on any environment or system 

Build simple docker react application following the video.

Build elt pipeline using docker 

How to Update the code and build another image.
Go through docker docs
What is mount point 
How to create multi container apps? How to connect multiple independent containers. Docker network 
